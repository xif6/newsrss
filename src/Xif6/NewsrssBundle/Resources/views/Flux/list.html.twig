<ol>
    {% for flux in fluxes %}
        <li>
            <a href="{{ path('xif6_newsrss_flux_index', { 'id': flux.id, 'slug': flux.slug }) }}">{{ flux.name }}</a>
        </li>
    {% endfor %}
</ol>